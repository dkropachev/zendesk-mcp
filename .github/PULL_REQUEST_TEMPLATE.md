## Summary

<!-- What changed and why? -->

## Security impact

<!-- Auth, credentials, redirects, filesystem access, or Zendesk writes affected? -->

## Checklist

- [ ] `gofmt` reports no changes
- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] Tests cover new behavior and failures
- [ ] No credentials, customer data, signed URLs, or binaries committed
- [ ] Documentation/changelog updated when user-visible
